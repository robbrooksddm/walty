setup:
  - corepack enable                     # gives us pnpm
  - pnpm config set store-dir .codex-cache/pnpm-store
  - pnpm install --offline --frozen-lockfile --ignore-scripts

test:
  - pnpm test --runInBand